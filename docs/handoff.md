# 交接单

两台机器（工程机 / 验收机）之间随时可能新开对话。这一页是**冷启动读的第一页**：
当前到哪儿了、下一步做什么、哪些事还悬着、哪些规矩不成文但踩过坑。

> 每次收档更新这一页。它只写"现在的状态"，不写历史 —— 历史在 `decisions.md` 与 `feedback.md`。

## 一、当前版本

- **v1.19.5** —— 搬迁归位（环境已恢复、绕法已撤）、新路径下第一次基线、
  §12 第五面：文档对代码说谎（D110 §12）
- 线上：https://geesonchan.github.io/game-of-life/
- 测试：**两个运行器都绿** —— Vitest **221 通过 / 0 失败**、JSC **221 通过 / 0 失败**
  （2026-09-02。基线那次是 220/220，第五面那条守卫是第 221 条）
- 未勾验收项：见 `acceptance.md` 里的 `- [ ]`

## 二、下次新开对话，先读这四份

1. 这一页
2. `docs/decisions.md` 的 **D110 最后几节**（当前到 §30）—— 近期的判据都在那儿
3. `docs/acceptance.md` 的**末尾两节 + 所有 `- [ ]`**
4. `docs/feedback.md` 的最后一轮 —— 作者最近一次的原话与口径

## 三、任务顺序（当前）

| 序 | 事 | 状态 |
| --- | --- | --- |
| 1 | 链接三批（分享 / 取景 / `id=` + `g=`） | 已完成 |
| 2 | ~~看展模式~~ | **已撤销（v1.19.0）**，判据留在 D110 §14/§17/§22/§25 |
| 3 | 坏链接的告知（外壳/原因/位置/一处文案/**独立窗口**） | **已完成**（v1.19.4，作者真机验过） |
| 4 | **撤销 / 后悔药** | **方案已定稿（见下）。开工前提已满足**：环境已恢复、两个运行器都绿（2026-09-02） |
| 5 | 链接压缩（`p=` / `pz=` 两条路） | 已评估：3.8×，救不了稠密局；decode 变异步，**启动闸已铺好路** |
| 6 | 外部评审第三批：图案补齐与分组 | 待做 |
| 7 | 外部评审第四批：第一个"做"的 Show | 待做 |

## 四、环境：**已恢复，绕法已撤**（2026-09-02）

项目已从 `~/Downloads/game-of-life` 移到 **`~/code/game-of-life`**，在新路径下重开会话，
沙箱权限随之恢复。核对过的四件事：

- 项目根、`public/`、`.github/`、`node_modules/` 全部读得到
- `npm` / `git` 在项目内直接启动即可，`git rev-parse --show-toplevel` 正常
- **Vitest 跑得起来**
- **两个运行器都绿**：Vitest **220 通过 / 0 失败**、JSC **220 通过 / 0 失败**。
  这是**新路径下的第一次基线**（此前那 220 条是在旧路径下跑绿的，不算数）

v1.19.2～v1.19.4 那一轮的三条绕法，**环境已恢复，绕法已撤**：

| 绕法 | 状态 |
| --- | --- |
| 测试：`/private/tmp/golrun` 符号链接农场 + `git show HEAD:<路径>` 取夹具 | **已撤** —— 目录已删；直接 `npm test` / `npm run test:jsc` |
| 提交：`git --git-dir=<repo>/.git --work-tree=<repo> …`，在 `/private/tmp` 下执行 | **已撤** —— 直接在项目内 `git` |
| 编辑：python 脚本 + 绝对路径 | **已撤** —— 常规编辑工具即可 |

以后再看到这三种写法，就是环境又降级了；**先修环境，不要先加绕法**。
"不是同一个工作环境"这一条判据留在第六节。

## 五、撤销 / 后悔药：方案定稿（环境已恢复，可照此开工）

- **范围**：橡皮擦、画笔、清空、随机填充、改尺寸、载入图案、载入内置局、读档
- **多步**，按**字节**封顶 **32 MiB**，超了丢最旧的（一步在橡皮擦场景下等于没有）
- **混合结构**：局部改动存补丁（`input.js` 里 D67 那套 `{cells:[[x,y,旧值]], runDirtyBefore}`
  已经现成，收笔时压栈即可）；整盘改动存 `captureSession` 整份快照
- **实测代价**：2304² 一份 5.06 MiB / 拷贝 1.1ms；1024² 1.00 MiB / 0.2ms；200² 39 KB；
  一笔涂抹约 270 条 ≈ 2 KB
- **补丁自带前提**（主判据）：记下 `w/h/规则记号/世代戳`，对不上就**先说再丢**其余栈，
  绝不按错坐标回滚。世代戳打在**闸那一处**（`assertBoardUnlocked`），
  于是将来新增的写盘路径自动被覆盖
- **兜底**：A 类（改尺寸/读档/载入）压栈即作废之前的补丁（保留，作为第二道）
- **"跑一代作废整栈"是另一处**：`engine.step()` **不过闸**（热路径不为一致性买单），
  所以它有自己的守卫；表旁边要留一行注释说明这件事
- **`canUndo()` 单一判断源**：按钮 `disabled`、临时条出不出现、撤销入口，三处都问它；
  守卫钉住 `disabled` 的赋值表达式就是 `!canUndo()`（§12 第一次落在布尔值上）
- **两个入口一次撤销**：常驻按钮（控制排，`class="rescue"` 救援蓝，手机上四颗各 88px）
  + 清空/随机之后画布下沿的临时条（44px，8 秒）。**同一个 token，消费一次两处都消失**
- **进下半张表**：`{ name: 'undo', writes: 'cells+env+view', keeps: 'replace', asks: false }`
- **还原要连** `currentShowId`（否则 `id=` 指错）**和"在不在跑"**
- **撤销自己不压栈**（守卫钉住）
- 兜底那句话要**具体**：「换了棋盘尺寸之后，之前的改动就退不回去了」，
  三种失效原因共用一个模板；不挡路，但必须有

## 六、冷启动七条（踩过坑才写下来的）

1. **推了才算交付。** 真机复验前先 `curl 线上页面 | grep app-version` 对版本号（D92）。
2. **验分支要照抄用户的复现步骤。** 他说"空盘直接点"，就不许先点随机填充（D105）。
3. **守卫写完必须红一次**，而且**匹配式必须唯一命中**（D110 §30）——
   四次假守卫里三次是"匹到了别的东西"。定位一律走 `locate()`，命中数不对就红。
   注释里对代码位置计数的话（"只有一处"/"唯一出口"）算**说的与做的**那一族的第五面，
   要在 `src/data/structure-claims.js` 登记（D110 §12）。
4. **验收打勾要在 commit 之前单独确认。**
5. **验按时间触发的东西，要用它在生产里真正收到的那个时钟**（D110 §26，差的是量纲）。
6. **时序冷启动：在"还没就绪"的那一刻验**（D110 §5）。
7. **数要量，不要猜**（D82）。

**"验的不是同一情形"这一族已经七个成员**：
不是同一版（D92）/ 不是同一条路（D105）/ 不是同一种人（D107）/ 不是同一个时刻（D110 §5）/
不是同一个会话阶段（§10）/ 不是同一个量纲（§26）/ **不是同一个工作环境**（v1.19.2 那次
在降级沙箱里推的线上，作者点名要记）。

## 七、跑起来

```bash
npm install && npm run dev      # 本地开发（别用 Bash 起服务，用预览工具）
npm test                        # vitest
npm run test:jsc                # JavaScriptCore 运行器，同一份 tests/cases.js
npm run build                   # 构建会把版本号盖进 <meta name="app-version">
```

浏览器里 `window.__lab` 就是整个 app。面板隐藏时 rAF 不跑，`handleResize()` 要手动调一次
（画布会停在 300×150 的出厂值 —— D110 §5 那个 bug 就是这么现形的）。
