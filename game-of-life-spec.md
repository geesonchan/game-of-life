# 生命游戏实验台 · 建造规格书（Build Spec for Claude Code）

> 用途：本文档是完整的项目规格。请按阶段顺序实现，每个阶段通过验收标准后再进入下一阶段。
> 每阶段完成后 git commit 一次，commit message 注明阶段号。

---

## 0. 项目概述

一个基于浏览器的 Conway 生命游戏**实验平台**（不只是玩具）。核心特性：

- 生动的视觉呈现（年龄着色、余晖、拖尾）
- 规则完全可编辑（条款列表编辑器，覆盖并超越 B/S 记法）
- 完整的数据记录（每代快照、死因统计、事件编年史、实验台账）
- 严格可复现（种子化随机、极简存档、确定性引擎）
- 规则勘探器（自动扫描规则空间，分类结局）
- RLE 标准格式互通（可导入 LifeWiki / Golly 社区图案）

## 1. 技术选型与约束

- **Vite + 原生 JavaScript**（不用 React，不用 TypeScript）。代码注释用中文。
- 渲染用 **HTML5 Canvas**（2D context）。禁止用 DOM 元素画格子。
- 无后端。所有数据在内存 + 文件下载/上传（JSON、RLE、CSV）。
- 不使用 localStorage / sessionStorage。
- 目标棋盘规模：默认 200×200，需流畅支持到 500×500。

## 2. 架构原则（不可违反）

1. **三层分离**：
   - `engine/`：纯逻辑。输入（棋盘, 规则, 边界类型）→ 输出下一代。零 DOM 依赖，可在 Node 里跑测试。
   - `render/`：只负责画。从引擎读状态，不修改状态。
   - `data/`：数据记录。挂在引擎的出生/死亡/换代事件钩子上，渲染层对其无感知。
2. **规则预编译**：任何规则（B/S 或条款列表）在应用前编译成查找表 `lookup[当前状态][活邻居数] → 下一状态`。引擎每代只查表，不解释规则。
3. **确定性**：v1 引擎完全确定。所有随机（初始化、勘探器采样）必须走**种子化 PRNG**（实现 mulberry32 或同等算法），种子记录在存档与台账中。
4. **双缓冲**：两个棋盘数组交替读写，禁止每代新建数组。
5. **数据与渲染节拍解耦**：高速模式下渲染可跳帧（如每 5 代画 1 次），数据记录每代必录。

## 3. 核心数据结构约定

### 3.1 存档格式（JSON，极简，靠重放恢复）
```json
{
  "version": 1,
  "seed": 4271,
  "initType": "random | pattern",
  "initDensity": 0.35,
  "initPattern": "RLE 字符串（initType=pattern 时）",
  "rule": { "type": "clauses", "clauses": [...], "fingerprint": "规则哈希" },
  "boundary": "torus | dead",
  "boardSize": [200, 200],
  "generation": 1847
}
```
读档 = 按种子重建初始盘 → 重放到 generation 代。

### 3.2 规则条款（clause）
```json
{ "when": "alive | dead | aging_1..N", "neighbors": { "op": "in", "values": [2,3] }, "then": "alive | dead | aging_1" }
```
- 条款从上到下匹配，第一条命中生效；列表末尾隐含兜底条款"维持原状"。
- 标准生命游戏 = 3 条条款（作为默认预填）。
- B/S 勾选框 UI 只是条款列表的语法糖，双向同步（能表达时）。

### 3.3 每代快照（内存数组，可导出 CSV）
`{ gen, alive, births, deathsLonely, deathsCrowded, activeArea }`
- 上限 50,000 条，超出后每 10 代抽稀 1 条（保留最近 5,000 条全量）。

### 3.4 实验台账（每局一条，可导出 CSV）
`{ runId, timestamp, seed, ruleFingerprint, ruleNotation, boundary, boardSize, initDensity, endType, endGen, peakPop, note }`
- `endType ∈ { extinction, still, cycle, capped, manual }`
- `note` 为用户手填备注栏。

## 4. 分阶段实现计划

### 阶段 1：引擎 + 渲染 + 基础控制
**功能**
- 标准规则 B3/S23（以条款查找表形式实现，但 UI 暂不开放编辑）
- 边界：环形（默认）/ 死边界，下拉切换
- Canvas 渲染，视口与棋盘分离：支持拖动平移、滚轮缩放
- 控制：播放/暂停、单步、速度滑块（1–60 代/秒）、清空、随机初始化（密度滑块 + 种子输入框，留空则随机生成种子并显示）
- 鼠标左键点击/拖画活细胞，右键擦除
- 帧率保护：单代计算超 16ms 自动降速；标签页切后台自动暂停

**验收标准（写成自动化测试，engine 层跑在 Node）**
- [ ] Blinker 周期为 2
- [ ] Glider 4 代后整体平移 (1,1)
- [ ] 环形边界：滑翔机穿过边缘后从对侧出现，形状不变
- [ ] 同一种子 + 同一密度，两次初始化棋盘逐格相同
- [ ] 500×500 随机盘（密度 0.3）单代计算 < 16ms

### 阶段 2：视觉增强
**功能**
- 年龄着色：新生亮色 → 随存活代数渐变加深（色带可从 2–3 个预设选）
- 死亡余晖：死亡细胞 3–5 代内渐隐残影
- 拖尾模式（开关）：叠加半透明层不完全清屏
- 统计面板：右侧实时显示当前代数 / 存活数 / 本代出生·死亡数（区分孤独死/拥挤死），及存活数折线图（最近 500 代）

**验收标准**
- [ ] 所有视觉效果均为渲染层实现，engine 目录零改动（git diff 验证）
- [ ] 开满所有效果后 200×200 仍保持 60fps

### 阶段 3：条款规则编辑器
**功能**
- 条款列表 UI：增/删/上下拖动排序，每条用下拉框选（当前状态、邻居条件、结果状态）
- 支持衰老态（Generations 族）：可设衰老层数 0–8
- B/S 快捷视图：18 个勾选框，与条款列表双向同步（条款超出 B/S 表达力时勾选框置灰并提示）
- 内置预设：Life (B3/S23)、HighLife (B36/S23)、Seeds (B2/S)、Brian's Brain (B2/S/C2)、Day & Night (B3678/S34678)
- 条款校验器：保存前检查覆盖完整性；永不可达的条款标灰提示
- 规则指纹：对编译后查找表求哈希，显示在界面上
- 规则导出/导入：能用 B/S 记法表达的用标准记法字符串，否则用条款 JSON

**验收标准**
- [ ] 用条款编辑器手工搭出 B3/S23，规则指纹与预设 Life 完全一致
- [ ] 切换到 Seeds 预设后运行，任意活细胞下一代必死（自动测试）
- [ ] 构造一条永不可达条款，校验器能标出

### 阶段 4：数据记录与编年史
**功能**
- 每代快照表（按 3.3），CSV 导出按钮
- 终止检测四条件（均可勾选启用）：全灭 / 静止（连续 2 代哈希相同）/ 循环（哈希与历史重复，报告周期长度）/ 代数上限（默认 10,000，可改）。哈希查重用 Map，禁止线性扫描。
- 触发终止 → 自动暂停 + 弹出本局总结卡片
- 事件编年史：自动记录里程碑（人口峰值、区域灭绝、进入循环等），时间线列表展示
- 实验台账（按 3.4）：每局终止后自动落一条，带备注输入框；台账页可查看、CSV 导出

**验收标准**
- [ ] 放一个 blinker 独跑，第 2 代检测到 cycle，周期报告为 2
- [ ] 空盘随机 3 个孤立细胞，检测到 extinction
- [ ] 快照 CSV 各列数值与界面统计一致（抽查 3 代）
- [ ] 循环检测在 10,000 代规模下无可感知卡顿

### 阶段 5：存档、种子与 RLE 互通
**功能**
- 存档/读档：按 3.1 格式，下载 JSON / 上传恢复。读档重放需显示进度条
- RLE 导入：支持标准 RLE 格式（含 `x, y, rule` 头行），可选放置位置（居中/点击放置）；图案大于棋盘时提示并裁剪
- RLE 导出：框选棋盘区域导出为 RLE 字符串（复制到剪贴板）
- 指纹回归测试：固定种子跑 1,000 代的棋盘哈希写入测试，作为引擎回归基线

**验收标准**
- [ ] 存档后刷新页面读档，棋盘逐格一致、代数一致
- [ ] 从 LifeWiki 复制 Gosper glider gun 的 RLE 粘贴导入，运行后每 30 代产出一个滑翔机
- [ ] 导出再导入同一图案，逐格一致

### 阶段 6：规则勘探器
**功能**
- 勘探模式页面：设定采样空间（B/S 全空间，或限定条款模板）、每规则试跑局数（默认 3 局不同种子）、每局代数上限（默认 2,000）
- 后台批量运行（用 Web Worker，不阻塞界面），进度显示
- 结局自动分类：速死（<50 代全灭）/ 爆炸（人口持续增长贴近密度上限）/ 静止 / 短周期循环 / **持续复杂**（跑满上限仍未终止且人口波动）
- 结果表：每规则一行（记法、指纹、3 局结局、平均终止代数），按"持续复杂"优先排序
- 候选名单：一键把有趣规则存入收藏，附首局种子，可直接跳转到主界面复现观看
- 勘探记录并入实验台账（每局一条）

**验收标准**
- [ ] 勘探器对 B3/S23 的分类结果为"持续复杂"或"长周期"（3 局中至少 2 局）
- [ ] 勘探器对 B2/S 的分类结果为"爆炸"
- [ ] 批量跑 100 个随机规则期间，主界面 UI 可正常交互

## 5. v1 明确不做（防止范围蔓延）

- 概率规则（破坏确定性，v2 再议）
- 多物种、资源层、目标模式关卡
- 结构级追踪（连通块识别与跨代身份匹配）——留作 v2 的核心特性
- 声音、六边形网格、无限画布（稀疏存储）
- 双宇宙对照视图（v2 优先项之一）

## 6. 测试与工程要求

- engine 层测试用 Vitest，在 Node 环境运行，覆盖各阶段验收标准中标注为自动测试的条目
- 每阶段的验收清单以 checklist 形式维护在 `docs/acceptance.md`，完成打勾
- 重要设计决策（如身份定义、哈希方案）落在 `docs/decisions.md`，一条一段
