// 整台机器：OTCA metapixel（D103）。
//
// **它的 RLE 不进首屏包。** 161 KB 的图案压进 index 包里，等于让每个只想看滑翔机的人
// 先下载它一遍。改成静态文件按需取：卡片上要显示的东西（名字、说明、生平、尺寸）
// 都在这儿声明，真正的格子等到点开那一刻才去拿。
//
// 声明 `w/h/cells` 的另一个用处：卡片要判断"当前这个盘摆不摆得下"，
// 有了这三个数就不必为了算尺寸先把 161 KB 解析一遍。
export const METAPIXEL_LAYOUT = Object.freeze({
  id: 'builtin:otca-metapixel',
  nameKey: 'fav.mp.otca',
  // 取自 LifeWiki 集合的镜像快照，#N/#O 署名原样保留在文件里（Brice Due, 2006）
  rleUrl: 'patterns/otca-metapixel.rle',
  w: 2058,
  h: 2058,
  cells: 64691,
  board: 2304,          // 元像素档：2058 + 四周各 123 格（D102 ①）
  boundary: 'dead',     // 死边界：它是一台机器，不该让自己绕回来撞自己
  scale: 'big',
  full: true,
  // 它要跑满一个元代才看得出在干什么 —— 35,328 代，2304² 上约半小时（数在下面的生平里）。
  // 分享它走 `id=builtin:otca-metapixel`：一条一百多字符的链接就能开（D110 §19）。
  // 生平按永动口径实测（与「永动喂食机」同一先例：报周期与人口，不报"终局代数"）。
  //
  // **但周期这一栏是空的，因为没量到。** 实测在 2304² 死边界上跑满 35,528 代
  // （一个元代 35,328 再多两百代），它**没有**回到起始盘面；人口始终在 54,337–65,280
  // 之间摆动。35,328 是这台机器设计上的"一个元代"，不是我们量出来的周期 ——
  // 单独一个元像素、四周没有邻居，未必从起始相位走一整圈回来。
  // 按 D82 的规矩：**没量到的不写进生平**，写"跑满未回到起点"。
  life: { end: 'capped', board: 2304, boundary: 'dead', start: 64691, gen: 35528,
    peak: 65280, peakGen: 0, final: 64202 }
})
