# 验收清单

> 勾选 = 已实现并验证。自动测试项见 `tests/cases.js`（Vitest 与 jsc 运行器共用同一批用例）。

## 阶段 1：引擎 + 渲染 + 基础控制

**自动测试（engine 层，Node 环境）**

- [x] Blinker 周期为 2
- [x] Glider 4 代后整体平移 (1,1)
- [x] 环形边界：滑翔机穿过边缘后从对侧出现，形状不变
- [x] 同一种子 + 同一密度，两次初始化棋盘逐格相同
- [x] 500×500 随机盘（密度 0.3）单代计算 < 16ms —— 实测中位 1.14ms（JavaScriptCore）/ 3.99ms（Chrome）

附加的自动测试（非验收标准要求，用于守住架构约定）：

- [x] 规则编译：B3/S23 查找表逐格正确，指纹稳定且不同规则不撞码
- [x] 死边界：界外恒按死细胞计算，且与环形边界结果确实不同
- [x] 统计钩子：出生 / 孤独死 / 拥挤死 计数正确

**功能项（浏览器内验证）**

- [x] 标准规则 B3/S23 以条款查找表实现（3 条条款），UI 只读展示记法与指纹
- [x] 边界下拉切换：环形（默认）/ 死边界
- [x] Canvas 渲染，视口与棋盘分离：拖动平移、滚轮以光标为锚点缩放（锚点漂移实测为 0）
- [x] 播放 / 暂停 / 单步 / 速度滑块 1–60 代每秒 / 清空
- [x] 随机初始化：密度滑块 + 种子输入框，留空则生成随机种子并回填显示
- [x] 鼠标左键拖画活细胞（Bresenham 补齐，快速拖动不漏格）、右键擦除
- [x] 帧率保护：单代计算超 16ms 自动降速并提示，恢复后自动解除
- [x] 标签页切后台自动暂停
- [x] 500×500 棋盘可正常渲染（实测单帧绘制 2.86ms）

## 阶段 2：视觉增强

**验收标准**

- [x] 所有视觉效果均为渲染层实现，engine 目录零改动
      —— `git diff --stat HEAD~1 -- src/engine` 输出为空；年龄与余晖状态住在 `src/render/visual-state.js`
- [x] 开满所有效果后 200×200 仍保持 60fps
      —— **前台浏览器实测 60fps 稳定（用户验收确认）**。
      辅助测量：每帧（引擎一代 + 视觉状态一代 + 渲染一帧）中位 **2.5ms** / p95 **3.2ms**，
      占 16.67ms 预算的约 15%；关掉全部效果为 1.9ms，拖尾图层的增量约 0.6ms。
      测量时每帧用 `getImageData` 读回一个像素强制刷完绘制管线，否则 Canvas2D 会把工作排队到 GPU、
      测出的 0.6ms 并不真实。

**功能项（浏览器内验证）**

- [x] 年龄着色：新生亮色 → 随存活代数渐变加深；3 个色带预设（翠绿 / 熔岩 / 冰川），亮度逐级单调递减
- [x] 死亡余晖：默认 4 代（可调 3–5）渐隐残影，亮度按 (d/n)^1.8 衰减，明显暗于活细胞
- [x] 拖尾模式：开关 + 长度滑块，走独立图层叠半透明层不完全清屏
- [x] 统计面板：代数 / 存活 / 出生 / 活动区 / 孤独死 / 拥挤死 实时显示
- [x] 存活数折线图（最近 500 代），带峰值参考线，主色跟随色带
- [x] 关闭全部效果时的画面与阶段 1 完全一致（同一条渲染路径）

## 阶段 3：条款规则编辑器

**验收标准**

- [x] 用条款编辑器手工搭出 B3/S23，规则指纹与预设 Life 完全一致
      —— 自动测试覆盖三种写法（`eq`/`range` 版、只写 2 条靠兜底补齐版、预设版），指纹均为 `9eba4f34`；
      界面里也实测：载入 HighLife → 点掉第 1 条条款的 chip「6」→ 指纹变为 `9eba4f34`；
      反方向（点 B/S 勾选框改条款）同样落到 `9eba4f34`
- [x] 切换到 Seeds 预设后运行，任意活细胞下一代必死（自动测试）
      —— 随机盘演化一代后上一代活细胞存活数为 0，并逐个邻居数查表确认 `lookup[活][n] = 死`
- [x] 构造一条永不可达条款，校验器能标出
      —— 三类分别测：被遮蔽 / 状态不可达 / 冗余；界面里新增条款被立刻标灰并给出原因，
      且删掉它指纹不变（证明它确实没有语义作用）

**功能项（浏览器内验证）**

- [x] 条款列表 UI：增 / 删 / ↑↓ / 拖动排序，当前状态、邻居条件、结果状态三个下拉
- [x] 邻居条件支持 `∈ / ∉ / = / 介于 / ≤ / ≥ / 任意`，集合用 0–8 chip 勾选
- [x] 衰老态（Generations 族）：层数 0–8，附「生成衰老链」一键补齐 衰老1→…→死亡
- [x] B/S 18 个勾选框与条款列表双向同步；超出表达力时置灰并**指名具体条款**
      （实测 Brian's Brain：「第 2 条条款会产生「衰老 1」，超出 B/S 记法只有生/死两态的表达力」）
- [x] 内置预设 5 个：Life / HighLife / Seeds / Brian's Brain / Day & Night
- [x] 条款校验器**与编辑器同期交付**：每次改动都重跑 结构校验 → 编译 → 语义校验，
      结构错误时禁用「应用规则」并逐条指出问题（越界的状态值在下拉里显示为「⚠ aging_1」，
      不会和错误提示自相矛盾）
- [x] 规则指纹实时显示在编辑器标题栏与主界面侧栏
- [x] 规则导出 / 导入：可表达时用 B/S 记法字符串，否则退化为条款 JSON；非法输入给可读错误
- [x] 编译表预览（状态 × 邻居数），靠隐含兜底决定的格子用虚线标出
- [x] 衰老态在画布上有独立配色（否则 Brian's Brain 的将死细胞会完全不可见）

## 阶段 3.5：双语与简洁模式（插入阶段）

**自动测试**

- [x] i18n 词典中英键集完全一致，且没有空词条（当前 191 条）
- [x] 中英占位符逐条一一对应（占位符对不上会在界面漏出 `{n}`，且不会报错）
- [x] 校验器可能产出的所有 key、每个图案与预设需要的 key，词典里都有
- [x] 5 个内置图案的尺寸与活细胞数正确（滑翔机 3×3/5、枪 36×9/36、脉冲星 13×13/48、
      轻量飞船 5×4/9、R-五连体 3×3/5）
- [x] 滑翔机放上棋盘后 4 代平移 (1,1)；脉冲星周期为 3；轻量飞船 4 代平移 2 格
- [x] 图案越界自动裁剪，居中放置坐标正确

**功能项（浏览器内验证）**

- [x] (a) i18n 词典机制：静态文字用 `data-i18n` 标注整树重刷，动态 HTML 走 `t()`；
      右上角 中/EN 切换，实测标题、按钮、HUD、下拉选项、placeholder、提示、
      规则编辑器（含打开状态下实时重刷）全部跟随切换
- [x] (b) 简洁/完整模式开关：简洁模式只保留 画笔 / 播放暂停 / 单步 / 速度 / 清空，
      外加图案盒子与世界卡片；统计、视觉、初始化、棋盘、规则五个区块与 HUD 的实速·帧率隐藏
- [x] 图案盒子：5 个内置图案带缩略图与一句话说明，点选后跟随鼠标（半透明预览 + 虚线框），
      点画布放置，可连续放置；右键或 Esc 取消
- [x] 实测滑翔机枪放置后每 30 代人口 +5（36→41→46→51→56），即每 30 代吐出一架滑翔机
- [x] (c) 世界卡片：经典 / 复制者 / 烟花 / 大脑 / 昼夜，每张一句话描述，
      点击即换规则，当前所在世界按**规则指纹**比对高亮

## 小阶段：文案与引导（插入阶段）

**自动测试**

- [x] 每个 `.simple` 白话词条都有对应的通用词条兜底（保证语域回落不会落空）
- [x] 简洁语域与三幕卡里不出现术语 —— 中英各一份禁用词表，扫描全部
      `*.simple` / `world.*` / `intro.*` 词条
- [x] 完整模式的术语提示必须是「术语 —— 一句人话」格式（强制含破折号）
- [x] 三幕卡与两页参考的词条齐全（36 个 key 逐个点名）
- [x] 每个控件的悬停提示都有词条（35 个 key 逐个点名）
- [x] 三条规矩的迷你棋盘演的确实是那三件事 —— 直接用引擎验：
      「朋友太少」一步全灭且两个都算孤独死；「挤得太满」中心变空、恰好 5 个拥挤死、
      不混进孤独死；「刚好三个」只诞生 1 个、结果是 2×2 方块

**功能项（浏览器内验证）**

- [x] ① 简洁模式文案大白话化，中英同步：开始 / 走一步 / 全部擦掉 / 走多快、
      玩具盒、会走路的小家伙、发射机器、会呼吸的花、横着飞的小船、五个格子的大麻烦；
      世界卡片改写为 普通世界 / 烟花世界 / 大脑世界 / 复印世界 / 黑白世界；
      HUD 改成「走了 / 还活着」。英文同口吻（a little guy that walks / a shooting machine …）
- [x] ② 三幕介绍卡：第一幕带自动演化的迷你舞台；第二幕三块**可点可走**的实验地；
      第三幕「开始玩」关卡并在空棋盘上放一个滑翔机（实测放下 5 格）
- [x] 可随时「跳过」；右上角「?」可重看；**首次进入才自动弹**
      （规格修订后"看过了"记在 localStorage，实测刷新不再弹）
- [x] 介绍卡自带中/EN 开关：首次弹出时卡片遮罩挡住了侧栏的语言开关，
      看不懂当前语言的人本来得先关掉卡片才能切换 —— 现在卡片上一键即可，且立刻记住
- [x] 完整模式的「?」额外两页：年龄颜色图例（色块直接取自渲染器的实际查找表）、
      B/S 白话解读（实测 B3/S23 解读为「B3 = 空格旁边刚好有 3 个活的，就诞生」
      「S23 = 活格旁边有 2 或 3 个活的，就活下去」）；简洁模式只有三幕
- [x] ③ 全控件悬停提示：33 个带提示的控件全部有 title，中英各一份，零缺失；
      完整模式术语控件为「术语 —— 一句人话」（如「环形 —— 从左边出去会从右边回来，像地球仪」）
- [x] 全部文字走 i18n 词典，HTML 里没有写死任何一句文案

## 规格修订：界面偏好持久化（localStorage 白名单）

**自动测试**

- [x] 白名单恰好是 `introSeen / lang / mode` 三样
- [x] 允许的键正常读写，全部带 `gol.pref.` 前缀，`clear()` 只清自己的键
- [x] 游戏数据键（board / cells / save / ledger / snapshot / seed / rule / generation /
      pattern / history / runs）读写**一律抛异常**，且存储里不留痕迹
- [x] 存储不可用（隐私模式）或读写抛异常时，读回落默认值、写返回 false，异常不逃逸

**功能项（浏览器内验证）**

- [x] 语言、简洁/完整模式、介绍卡已看过 —— 三样刷新后都被记住，开关高亮同步恢复
- [x] 实测：切到 EN + 简洁 → 刷新 → 仍是 EN + 简洁，按钮显示「▶ Start」，介绍卡不再弹出
- [x] 实测存储里只有三个 `gol.pref.*` 键，没有任何棋盘/规则/种子数据

## 插入阶段：布局重构（来自首位用户实测反馈）

**约束核对**

- [x] ① 纯 UI 层，engine 与 data 零改动 —— `git diff --stat HEAD -- src/engine src/data` 输出为空；
      33 条测试原样全过
- [x] ② 简洁模式下顶栏只显示该模式的控件，data-mode 机制照旧 —— 实测简洁模式顶栏为
      开始 / 走一步 / 走多快 / 全部擦掉 / 玩具盒 / 世界 / 模式 / 语言 / ?，右栏只剩「怎么玩」
- [x] ③ 全部文字沿用现有词典键，**零新增文案** —— 词典 diff 只有 `app.subtitle` 的阶段号，
      词条数保持 292 / 292
- [x] ④ 右栏为阶段 4 的台账/编年史预留了分组位（`#slot-records`，隐藏，位置在统计与规则之后、
      操作提示之前 —— 同属"读"的内容）
- [x] ⑤ iPad 宽度（768px）实测：顶栏折行为两行（70px）、卡片条横向滚动，
      画布起点在顶栏之下，`stage.top < topbar.bottom` 为 false，不遮画布

**功能项（浏览器内验证）**

- [x] 顶栏左段：播放/暂停、单步、速度、清空（清空归此段的理由见 D32）
- [x] 顶栏中段：「图案盒子」「世界」两个标签，点击向下展开横向卡片条，再点收起，
      同一时刻只开一条；卡片条横向滚动，不用 select
- [x] 顶栏右段：简洁/完整、中/EN、「?」
- [x] 右栏只剩"读和调"：棋盘（尺寸/边界/密度/种子）、视觉、统计，完整模式追加规则；
      分组标题可点击折叠
- [x] 边界（2 项）、色带（3 项）、尺寸（4 项）三个 select 全部改成按钮组平铺（D33）
- [x] 中央画布吃掉其余全部空间；卡片条展开/收起时保持"原本在正中的格子仍在正中"，
      既不裁掉棋盘也不冲掉用户的缩放

## 插入阶段：布局微调（首位用户第二轮反馈，记录见 [feedback.md](./feedback.md)）

**约束核对**

- [x] ③ 纯 UI 层 —— 只动了 `index.html` / `style.css` / `controls.js` / `main.js` / `library.js`；
      `git diff --stat HEAD -- src/engine src/data` 输出为空，测试 33/33 原样通过
- [x] ③ 零新词条 —— `git diff --stat HEAD -- src/i18n/` 输出为空；
      工具条的名称与提示沿用 `pattern.<key>` / `pattern.<key>.desc` / `pattern.hint`
- [x] ③ 窄屏约束照旧 —— 768px 实测：顶栏 44px 单行、工具条收窄到 88px、
      世界横条横向滚动；`stage.top < topbar.bottom` 与 `stage.left < rail.right` **均为 false**

**功能项（浏览器内验证）**

- [x] ① 玩具盒改为画布左缘竖排工具条：缩略图 + 名称，点顶栏「玩具盒」展开、再点收起；
      选中态高亮（实测点脉冲星后该按钮高亮、提示条显示「已选中 脉冲星 —— 点画布放置」）
- [x] ① 简洁模式下工具条常驻展开，且「玩具盒」标签本身隐藏（常驻的东西不需要开关）
- [x] ① 世界卡片维持顶部横条不变；两者占不同的边，可同时展开（实测两个标签同时高亮）
- [x] ② 顶栏改为：品牌 / 清空 ‖ 玩具盒 · 世界 ‖ …… ‖ 播放 · 单步 · 速度 ‖ 模式 · 语言 · ?
- [x] 工具条是工作区的布局成员而非浮层 —— 展开时画布变窄，不会盖住画布

## 阶段 4：数据记录与编年史

**验收标准**

- [x] blinker 独跑，第 2 代检测到 cycle，周期报告为 2
      —— 自动测试与浏览器实测均为 `{type:'cycle', gen:2, from:0, period:2}`
- [x] 空盘随机 3 个孤立细胞，检测到 extinction —— 第 1 代全灭，棋盘确实清空
- [x] 快照 CSV 各列数值与界面统计一致（抽查 3 代）
      —— 自动测试抽查第 1 / 60 / 120 代逐列比对；浏览器里另跑一局 2160 代，
      导出 CSV 的末行与统计面板六个数字逐个相同
- [x] 循环检测在 10,000 代规模下无可感知卡顿
      —— 跑满 10,000 代实测每代额外开销 **0.012ms**（裸跑 0.026 → 带哈希+检测 0.038）；
      查重结构的复杂度守卫：条数 ×5 时耗时仅 ×3.4（线性扫描应接近 ×25）

**功能项（浏览器内验证）**

- [x] 每代快照表按规格 3.3：最近 5,000 条全量、更早每 10 代 1 条、上限 50,000；
      越界时粒度自动放大（实测 4 万代压到 139 条，最近窗口仍是全量）
- [x] 快照 CSV 导出，列名与规格 3.3 一致
- [x] 终止检测四条件各自可勾选：全灭 / 静止 / 循环 / 代数上限（默认 10,000，可改）
- [x] 哈希查重用 `Map`，禁止线性扫描 —— 有测试断言 `detector.seen instanceof Map` 并守住复杂度
- [x] 触发终止 → 自动暂停 + 弹出本局总结卡片
- [x] 总结卡片双语域：完整模式给精确描述（「第 2160 代回到了第 2158 代的状态，周期 2 代」），
      简洁模式给儿童版（「**这个世界走了 3 步后开始原地转圈，每 3 步重复一次**」）
- [x] 事件编年史时间线：开局 / 人口峰值 / 大幅回落 / 安静下来 / 终止，按代数排列并分色
- [x] 实验台账按规格 3.4：每局终止后自动落一条，带备注输入框（总结卡片与台账列表都能填），
      可查看、可 CSV 导出、可清空
- [x] 全部新界面文字走词典且含简洁语域；面板归入当初预留的 `#slot-records`

## 阶段 5：存档、种子与 RLE 互通
- [ ] 存档后刷新页面读档，棋盘逐格一致、代数一致
- [ ] Gosper glider gun 的 RLE 导入后每 30 代产出一个滑翔机
- [ ] 导出再导入同一图案，逐格一致

## 阶段 6：规则勘探器
- [ ] B3/S23 分类为"持续复杂"或"长周期"（3 局中至少 2 局）
- [ ] B2/S 分类为"爆炸"
- [ ] 批量跑 100 个随机规则期间，主界面 UI 可正常交互
